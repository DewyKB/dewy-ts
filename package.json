{
    "name": "dewy_ts",
    "version": "0.0.1",
    "private": false,
    "scripts": {
        "generate-client": "openapi --name Dewy -c axios --input http://localhost:8000/openapi.json --output ./src/client --useUnionTypes",
        "wtf": "openapi --help"
    },
    "dependencies": {
        "axios": "^1.6"
    },
    "devDependencies": {
        "openapi-typescript-codegen": "^0.26",
        "typescript": "^4.6.2"
    }
}
